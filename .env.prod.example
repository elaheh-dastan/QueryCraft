# ============================================
# QueryCraft Production Environment
# ============================================
# This file contains production-specific configuration.
# Copy this to .env before deploying to production.
#
# IMPORTANT: All values marked as REQUIRED must be changed!
# ============================================

# ==========================================
# Django Configuration (REQUIRED)
# ==========================================

# REQUIRED: Generate a strong secret key
# Run: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=CHANGE-THIS-TO-A-RANDOM-SECRET-KEY

# REQUIRED: Disable debug in production
DEBUG=0

# REQUIRED: Set your domain names (comma-separated)
# Example: ALLOWED_HOSTS=example.com,www.example.com,api.example.com
ALLOWED_HOSTS=your-domain.com

# ==========================================
# Database Configuration
# ==========================================

POSTGRES_DB=querycraft
POSTGRES_USER=querycraft

# REQUIRED: Change default password
POSTGRES_PASSWORD=CHANGE-THIS-TO-A-SECURE-PASSWORD

# Don't expose PostgreSQL port in production (leave empty or comment out)
# POSTGRES_PORT=

# ==========================================
# Ollama Configuration
# ==========================================

# Don't expose Ollama port in production if not needed externally
# OLLAMA_PORT=

# Model name
OLLAMA_MODEL_NAME=sqlcoder-7b-2:local

# ==========================================
# Web Service Configuration
# ==========================================

# Web port (default: 8000)
WEB_PORT=8000

# Don't seed database in production
SEED_DB=false

# ==========================================
# Security Settings (RECOMMENDED)
# ==========================================

# CSRF trusted origins (required if behind proxy/load balancer)
# CSRF_TRUSTED_ORIGINS=https://example.com,https://www.example.com

# Session cookie secure (HTTPS only)
# SESSION_COOKIE_SECURE=true

# CSRF cookie secure (HTTPS only)
# CSRF_COOKIE_SECURE=true

# ==========================================
# Email Configuration (OPTIONAL)
# ==========================================

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=1
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password

# ==========================================
# Monitoring & Logging (OPTIONAL)
# ==========================================

# LOG_LEVEL=INFO

# ==========================================
# PRODUCTION CHECKLIST
# ==========================================
#
# Before deploying, ensure you have:
# ✓ Changed SECRET_KEY to a random string
# ✓ Set DEBUG=0
# ✓ Updated ALLOWED_HOSTS with your domains
# ✓ Changed POSTGRES_PASSWORD
# ✓ Reviewed and updated all REQUIRED fields above
# ✓ Removed or commented out unnecessary port exposures
# ✓ Set up HTTPS/SSL certificates (if applicable)
# ✓ Configured backups for database
# ✓ Set up monitoring and logging
#
# Run validation: just prod-validate
# ==========================================
